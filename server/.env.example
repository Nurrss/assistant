# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Google Cloud Speech-to-Text
# Option 1: Local file (for development)
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# Option 2: Base64 encoded (for deployment on Render, Vercel, etc.)
# Run: cat google-credentials.json | base64
# Then paste the output here:
# GOOGLE_CREDENTIALS_BASE64=your_base64_encoded_credentials_here

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# Narakeet Text-to-Speech API
NARAKEET_API_KEY=your_narakeet_api_key_here

# Rate Limiting (requests per 15 minutes per IP)
RATE_LIMIT_MAX=50
